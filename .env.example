# Balans Psikoloji - Environment Variables
# Bu dosyayı .env olarak kopyalayın ve değerleri kendi projenize göre düzenleyin

# Supabase Configuration (Zorunlu)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# API Configuration (Opsiyonel)
VITE_API_URL=https://your-api-url.com

# Debug Mode (Opsiyonel - Production'da false olmalı)
VITE_ENABLE_DEBUG=false

# Not: Bu dosyayı .env olarak kopyalayın ve gerçek değerlerle doldurun
# .env dosyası git'e commit edilmemelidir (güvenlik için)